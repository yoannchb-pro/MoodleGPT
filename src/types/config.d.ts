type Config = {
  apiKey: string;
  code: string;
  model?: string;
  infinite?: boolean;
  typing?: boolean;
  mouseover?: boolean;
  cursor?: boolean;
  logs?: boolean;
  title?: boolean;
  table?: boolean;
  timeout?: boolean;
};

export default Config;
